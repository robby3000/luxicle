NEXT_PUBLIC_SUPABASE_URL="your-supabase-url-here"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Optional: For server-side operations if you need a service role key (handle with extreme care)
# SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key-here"

# NextAuth.js variables (if you plan to use NextAuth with Supabase as a database, not as an auth provider directly)
# NEXTAUTH_URL="http://localhost:3000" # Or your production URL
# NEXTAUTH_SECRET="your-nextauth-secret-here" # Generate a strong secret
